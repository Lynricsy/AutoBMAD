{
  "name": "autobmad",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "autobmad": "./src/cli/index.ts"
  },
  "scripts": {
    "build": "bun build src/cli/index.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "start": "bun run src/cli/index.ts"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
